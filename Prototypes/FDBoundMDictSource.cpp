#include "FDBoundMDictSource.h"

// The implementation was promoted to MDictCore/ManagedMDictSource.cpp. This
// translation unit remains only so existing prototype-oriented source lists
// fail visibly if they are not updated to link the production primitive.
