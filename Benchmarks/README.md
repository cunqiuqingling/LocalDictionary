# Benchmarks

Benchmark outputs and the generated SQLite index are machine-local and ignored by Git. Phase 2 records first indexing, unchanged-source reuse, per-word latency, RSS, cache limits, and process disk I/O against the selected real Oxford dictionary.
